#include "Grid.h"

Grid::Grid(double size) {
    createGrid(size);
}

void Grid::createGrid(double size) {
    std::vector<Vertex> vertices;
    std::vector<unsigned int> inidces;

    for(int i = 0; i < size; i++) {
        for(int j = 0; j < size; j++) {
            
        }
    }
}